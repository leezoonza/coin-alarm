services:
  mysql:
    image: 'mysql:8.4.9'
    environment:
      - 'MYSQL_DATABASE=coin-alarm'
      - 'MYSQL_PASSWORD=secret'
      - 'MYSQL_ROOT_PASSWORD=verysecret'
      - 'MYSQL_USER=user'
    ports:
      - '3306:3306'
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data: